import {  useRoute } from "@react-navigation/native";
import { Text } from "react-native";

export const PostsScreen = () => {
	// const route = useRoute();
	// const { newPost } = route.params;

	// console.log(newPost)

	return <Text>dsv</Text>;
};
